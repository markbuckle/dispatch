import { EmptyState } from '../empty-state';

export default function TemplatesPage() {
  return (
    <EmptyState
      title="No templates yet"
      body="Template creation hasn't shipped. You'll build reusable email templates here."
    />
  );
}
