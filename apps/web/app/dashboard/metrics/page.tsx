import { EmptyState } from '../empty-state';

export default function MetricsPage() {
  return (
    <EmptyState
      title="No metrics yet"
      body="Delivery metrics will show up here once email is actually sending."
    />
  );
}
